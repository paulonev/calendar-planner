import { initCalendar } from "./calendar.js";
import "./css/main.scss";

initCalendar();

// const express = require("express");

// const app = express();
// const portNum = process.env.PORT || 8888;

// app.get('/', (req, res) => {
//     // write here your code to process request and send response on matching / route
// });

// app.listen(portNum, () => {})